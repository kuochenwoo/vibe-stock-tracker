# 2026-03-30

## 2026-03-30 08:56:14

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Modular Backend And Managed Tickers

### STAR

#### Situation
The backend market fetch path was failing, the project structure was too concentrated, and tracked instruments were still tied to hard-coded assumptions.

#### Task
Make the app behave like a real project by modularizing the backend, separating providers, and supporting managed tracked tickers.

#### Action
- Refactored the backend into clearer `api`, `core`, `models`, `providers`, `repositories`, and `services` layers.
- Added a provider factory and moved market access behind provider abstractions.
- Replaced hard-coded ticker placeholders with managed tracked tickers and added ticker management endpoints and frontend support.
- Updated Postman and README to match the backend contract.

#### Result
The backend became easier to extend, tracked instruments became configurable, and the app could evolve toward additional providers cleanly.

### Reason
These changes were made to move the app from a prototype structure to a maintainable project architecture.

## 2026-03-30 09:47:03

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Dashboard Frontend Expansion

### STAR

#### Situation
The frontend layout was cramped, management flows were overly static, and the app needed a more serious dashboard experience.

#### Task
Reshape the frontend into a cleaner dashboard while improving ticker management, alarm workflows, accessibility on a LAN, and market sentiment visibility.

#### Action
- Reworked the page header, settings entry point, ticker management modal, and alarm workflows.
- Added LAN-friendly frontend/backend access changes and improved browser-side notification handling.
- Added the Fear & Greed gauge and related backend/frontend support.
- Introduced a sidebar/news placeholder structure and refined the overall dashboard composition.

#### Result
The app became more usable as a dashboard, with better layout, better controls, and stronger support for live monitoring and future expansion.

### Reason
These changes were made to turn the UI into a more practical monitoring surface instead of a rough prototype.

## 2026-03-30 11:35:41

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Ticker Card And Interaction Polish

### STAR

#### Situation
The card design, alarm entry flow, modal presentation, and collapsed ticker watchlist behavior still needed significant refinement to feel usable and visually coherent.

#### Task
Polish the frontend interactions and visual structure, especially the ticker cards and compact watchlist behavior.

#### Action
- Reworked the ticker cards, chart treatment, price markers, and compact/collapsed states.
- Moved alarm creation into per-ticker controls and refined the drawer workflow.
- Simplified and restyled the tracked tickers modal, hints, and settings controls.
- Refined collapsed watchlist alignment so prices, controls, and ticker badges behave more like a finance watchlist.

#### Result
The frontend now behaves more like a focused market dashboard, with cleaner card interactions and a more intentional compact ticker layout.

### Reason
These changes were made to improve usability, readability, and visual consistency across the core market-monitoring flows.

## 2026-03-30 13:47:10

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Daily Changelog Workflow

### STAR

#### Situation
The changelog process was creating too many low-signal entries, and the repository was still relying on a single growing root changelog file.

#### Task
Reduce changelog noise, keep logs easier to navigate, and define a durable structure for future entries.

#### Action
- Updated the workflow skill to batch minor related tweaks into larger changelog entries when appropriate.
- Replaced the monolithic root changelog with a root index plus daily log files under month-based folders.
- Preserved the current history as daily files for March 2026.

#### Result
The project now has a cleaner changelog workflow and a file structure that is easier to maintain over time.

### Reason
These changes were made to keep project history readable and to avoid a single oversized changelog file.

## 2026-03-30 13:55:45

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Database Infrastructure Planning

### STAR

#### Situation
The project needed to move from JSON-based tracked ticker storage toward a more durable architecture, starting with Postgres for ticker definitions and Redis for short-lived market reference values.

#### Task
Create the initial infrastructure files needed to stand up Postgres and Redis locally, define the Postgres schema for tracked tickers and aliases, and document the Redis key design for runtime market state.

#### Action
- Added [docker-compose.yml](/Users/guozhen_wu/Documents/vibe-code-test/docker-compose.yml) with local Postgres and Redis services, health checks, and persistent volumes.
- Added [001_market_schema.sql](/Users/guozhen_wu/Documents/vibe-code-test/infra/postgres/init/001_market_schema.sql) to define `tracked_tickers` and `ticker_aliases`, including seed records for crude oil and gold futures.
- Added [redis-key-design.md](/Users/guozhen_wu/Documents/vibe-code-test/docs/redis-key-design.md) to document the runtime Redis keys for latest quotes and previous completed 5-minute closes.
- Updated [README.md](/Users/guozhen_wu/Documents/vibe-code-test/README.md) with the new local database infrastructure entry points.

#### Result
The repository now has a concrete local database stack definition and a first-pass data design for both durable ticker metadata and short-lived Redis market state.

### Reason
These changes were made to establish a clear starting point for moving tracked tickers into Postgres and 5-minute reference values into Redis before wiring the application code to those stores.

## 2026-03-30 14:11:43

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Postgres And Redis Backend Integration

### STAR

#### Situation
The repository already had the database design and local infrastructure files, but the running backend still used a JSON file for tracked tickers and did not actually write runtime quote state into Redis.

#### Task
Replace the JSON-backed ticker persistence with Postgres, connect the backend to Redis for latest quote and previous completed 5-minute close storage, and preserve the current API behavior for the frontend.

#### Action
- Added [database.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/core/database.py) to manage Postgres connections and bootstrap the schema.
- Added [cache.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/core/cache.py) to write latest quotes and previous completed 5-minute closes into Redis.
- Replaced the JSON ticker repository with a Postgres-backed [ticker_repository.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/repositories/ticker_repository.py).
- Extended [market.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/models/market.py) and [ticker_service.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/services/ticker_service.py) to support provider, aliases, and metadata on tracked tickers.
- Updated [market_service.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/services/market_service.py), [dependencies.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/api/dependencies.py), and [main.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/main.py) so the app initializes Postgres-backed ticker storage and writes runtime market cache state into Redis.
- Updated [yfinance_provider.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/providers/yfinance_provider.py) and [mock_provider.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/providers/mock_provider.py) so each quote now carries the previous completed 5-minute close metadata needed for Redis.
- Added `psycopg` and `redis` dependencies in [requirements.txt](/Users/guozhen_wu/Documents/vibe-code-test/backend/requirements.txt) and updated [README.md](/Users/guozhen_wu/Documents/vibe-code-test/README.md) to document the new runtime configuration.

#### Result
The backend now persists tracked tickers in Postgres and writes runtime market cache values into Redis, while keeping the existing ticker and market APIs intact for the frontend.

### Reason
These changes were made to move the backend onto the intended database architecture so tracked instruments are durable and runtime quote references are stored in an appropriate fast cache.

## 2026-03-30 14:15:03

### <span style="color:#d14343;font-weight:700;">FIX</span> Postgres Ticker Metadata Insert

### STAR

#### Situation
After moving tracked tickers into Postgres, creating a new ticker through `POST /api/tickers` could fail with `psycopg.ProgrammingError` because the repository was passing a raw Python `dict` into a JSONB column.

#### Task
Fix the ticker insert path so request metadata is written to Postgres in a format `psycopg` can adapt.

#### Action
- Updated [ticker_repository.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/repositories/ticker_repository.py) to wrap ticker metadata with `Jsonb(...)` before inserting into the `tracked_tickers.metadata` column.

#### Result
The backend can now insert new tracked tickers with JSON metadata without throwing the JSON adaptation error.

### Reason
These changes were made to restore working ticker creation after the Postgres migration.

## 2026-03-30 14:17:29

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Formalized Changelog Entry Types

### STAR

#### Situation
The changelog workflow already supported daily files and batched updates, but it did not yet define a formal distinction between feature entries and bug-fix entries.

#### Task
Standardize the changelog format so feature work is grouped into one main feature entry and bug fixes are grouped into concise fix entries.

#### Action
- Updated [SKILL.md](/Users/guozhen_wu/Documents/vibe-code-test/skills/change-delivery-workflow/SKILL.md) to define `<span style="color:#1b8f59;font-weight:700;">FEAT</span>` entries for grouped feature work and `<span style="color:#d14343;font-weight:700;">FIX</span>` entries for grouped bug fixes.
- Updated [openai.yaml](/Users/guozhen_wu/Documents/vibe-code-test/skills/change-delivery-workflow/agents/openai.yaml) so the workflow metadata matches the new categorized changelog format.
- Updated [CHANGELOG.md](/Users/guozhen_wu/Documents/vibe-code-test/CHANGELOG.md) to document the new changelog rules in the root index.

#### Result
The repository now has a formal changelog convention: grouped feature work is recorded as a main `FEAT` entry with STAR, and grouped bug fixes are recorded as concise `FIX` entries.

### Reason
These changes were made to make the changelog easier to scan and more consistent across larger features and smaller bug-fix batches.

## 2026-03-30 14:18:49

### <span style="color:#d14343;font-weight:700;">FIX</span> Historical Changelog Labels

- Updated the existing March changelog entries to use the new categorized labels.
- Converted grouped feature batches to `<span style="color:#1b8f59;font-weight:700;">FEAT</span>` headings.
- Converted the Postgres ticker insert bug entry to a `<span style="color:#d14343;font-weight:700;">FIX</span>` heading.

## 2026-03-30 14:24:23

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Frontend Panel Controls

### STAR

#### Situation
The market dashboard still lacked direct controls for changing ticker order, and the Fear & Greed panel always occupied its full card footprint even when only the current sentiment value was needed.

#### Task
Add lightweight panel controls that let users reorder ticker cards directly from the dashboard and collapse the Fear & Greed card into a compact value-only state.

#### Action
- Updated [useMarketStream.js](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/composables/useMarketStream.js) to support local ticker reordering with persisted order state.
- Updated [App.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/App.vue) and [MarketCard.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/components/MarketCard.vue) to add up/down reorder controls to each ticker panel.
- Updated [FearGreedGauge.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/components/FearGreedGauge.vue) to add a collapse toggle and a compact colored value-only view.
- Updated [styles.css](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/styles.css) to style the reorder controls and the compact Fear & Greed state.

#### Result
Users can now reorder ticker panels directly from the page, and the Fear & Greed card can collapse into a smaller footprint while still showing the colored current value.

### Reason
These changes were made to give the dashboard more direct control over panel organization and reduce sidebar space usage when only a compact sentiment signal is needed.

## 2026-03-30 14:29:48

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Drag Reordering And Compact Sentiment Header

### STAR

#### Situation
The initial reorder controls still relied on up/down buttons instead of a direct drag-and-place interaction, and the collapsed Fear & Greed card still did not present its title, source link, and value in the compact single-line form the dashboard needed.

#### Task
Replace button-based panel movement with drag-and-drop reordering and refine the Fear & Greed collapsed header into a tighter single-line layout.

#### Action
- Added animated drag-and-drop support via [package.json](/Users/guozhen_wu/Documents/vibe-code-test/frontend/package.json) and [package-lock.json](/Users/guozhen_wu/Documents/vibe-code-test/frontend/package-lock.json) using `vuedraggable`.
- Updated [MarketCard.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/components/MarketCard.vue), [App.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/App.vue), and [useMarketStream.js](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/composables/useMarketStream.js) so the whole ticker card is drag-and-droppable, while action controls are excluded from drag interaction.
- Updated [FearGreedGauge.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/components/FearGreedGauge.vue) so the title itself is the hyperlink with `CNN` as tooltip text, and the compact state keeps the title on the left with the colored value on the right while the collapse toggle stays at the upper right.
- Updated [styles.css](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/styles.css) to add drag animation states, disable panel text selection during drag interaction, and support the tighter compact sentiment header layout.

#### Result
Ticker panels can now be repositioned with animated drag-and-place behavior by dragging the card itself, and the collapsed Fear & Greed card now uses a cleaner single-line presentation where the title is clickable, `CNN` is preserved as tooltip text, and the value sits on the right.

### Reason
These changes were made to make panel organization feel more natural and to reduce visual friction in the compact sentiment view.

## 2026-03-30 14:45:11

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Panel Order Preference Sync

### STAR

#### Situation
Ticker panel order was only stored in browser local storage, so the dashboard had no durable backend record of the preferred layout, and the Fear & Greed source hint still relied on a fragile native tooltip behavior that was not reliably appearing.

#### Task
Keep panel order fast and local in the browser, add periodic persistence to the backend database, and make the Fear & Greed source hint visibly reliable on hover.

#### Action
- Added a Postgres-backed `app_preferences` table and new `/api/preferences/panel-order` endpoints to read and store panel order independently from ticker metadata.
- Updated [useMarketStream.js](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/composables/useMarketStream.js) so panel order still saves immediately to `localStorage`, hydrates from the backend only when local state is empty, and syncs back to the backend once per hour.
- Replaced the native title-only Fear & Greed source hint with an explicit hover tooltip in [FearGreedGauge.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/components/FearGreedGauge.vue) and [styles.css](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/styles.css).
- Updated [postman_collection.json](/Users/guozhen_wu/Documents/vibe-code-test/postman_collection.json) for the new preferences API.

#### Result
Panel order remains responsive in the browser, now has a durable backend sync path, and the Fear & Greed source tooltip is shown reliably on hover.

### Reason
These changes were made to keep the dashboard preference flow lightweight for the UI while still preserving layout state in the database over time.

## 2026-03-30 14:51:36

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Persistent Alarm Rules And Card Summaries

### STAR

#### Situation
Alarm rules were still stored only in browser state, so a page refresh removed them, and the dashboard did not show enough per-card alarm context beyond opening the drawer.

#### Task
Move alarms into the backend database, expose them through API endpoints, and make alarm state visible directly on ticker cards with a flatter visual marker and a concise summary when expanded.

#### Action
- Added a Postgres-backed `alert_rules` table and new backend alarm repository/service flow in [alert_repository.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/repositories/alert_repository.py) and [alert_service.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/services/alert_service.py).
- Added `GET /api/alerts`, `POST /api/alerts`, and `DELETE /api/alerts/{alert_id}` in [markets.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/api/routes/markets.py), and updated [postman_collection.json](/Users/guozhen_wu/Documents/vibe-code-test/postman_collection.json).
- Reworked [useAlerts.js](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/composables/useAlerts.js) so the frontend now loads, creates, and deletes alarms through the backend instead of `localStorage`.
- Updated [MarketCard.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/components/MarketCard.vue) and [styles.css](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/styles.css) to show a flatter alarm marker beside the current price and an expanded-card summary of active alarm thresholds.
- Updated [README.md](/Users/guozhen_wu/Documents/vibe-code-test/README.md) to document the persisted alert behavior and new endpoints.

#### Result
Alarm rules now survive refreshes, the frontend uses the backend as the source of truth, and each ticker card shows clearer alarm state without forcing the drawer open.

### Reason
These changes were made to turn alerts into durable application data instead of browser-only state and to make active alarm context easier to read directly from the dashboard.

## 2026-03-30 14:53:42

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Alarm Management Surface Refinement

### STAR

#### Situation
The new alarm indicator still read too much like text instead of an icon, the expanded ticker cards carried extra alarm management controls, and there was no single place to review all alarms globally from settings.

#### Task
Refine the alarm UI so the card marker is icon-based, the price card only shows a concise summary, and settings exposes a global alarm management view.

#### Action
- Replaced the text-based card marker with a compact SVG bell icon in [MarketCard.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/components/MarketCard.vue).
- Simplified the expanded ticker-card alarm area to show only a concise summary of active alarms instead of additional action controls.
- Added [AlertsManager.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/components/AlertsManager.vue) and exposed it through a new `All Alarms` entry in the settings menu via [App.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/App.vue).
- Updated [styles.css](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/styles.css) so the bell marker and summary treatment stay visually quiet and consistent with the rest of the dashboard.

#### Result
Alarm state now reads more cleanly on the cards, and the dashboard includes a single global place to review and delete alarms without going ticker by ticker.

### Reason
These changes were made to keep the price panels focused while still making alarm management easy to reach from the main dashboard controls.

## 2026-03-30 15:28:53

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Session-Aware Price History Seeding

### STAR

#### Situation
The price panels were still drawing their `1D` lines mostly from browser-captured live prices after page load, which meant refreshes started with weak chart context and there was no asset-aware definition of where a chart day should begin.

#### Task
Introduce a proper seeded chart-history path that starts in the database model, applies asset-specific session defaults in the backend, exposes a history API, and lets the frontend seed each price panel before merging live updates.

#### Action
- Extended the data model in [001_market_schema.sql](/Users/guozhen_wu/Documents/vibe-code-test/infra/postgres/init/001_market_schema.sql) and the default ticker metadata so futures now carry session-aware defaults in Postgres metadata.
- Added chart-history models in [market.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/models/market.py), provider support in [base.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/providers/base.py), [yfinance_provider.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/providers/yfinance_provider.py), and [mock_provider.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/providers/mock_provider.py), and Redis caching in [cache.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/core/cache.py).
- Added backend history normalization and caching in [market_service.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/services/market_service.py) with anchored session defaults:
  - futures: `18:00 America/New_York`
  - crypto: `00:00 UTC`
  - stocks: `04:00 America/New_York`
- Added `GET /api/markets/{code}/history` in [markets.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/api/routes/markets.py) and updated [postman_collection.json](/Users/guozhen_wu/Documents/vibe-code-test/postman_collection.json).
- Updated [useMarketStream.js](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/composables/useMarketStream.js) and [MarketCard.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/components/MarketCard.vue) so price panels seed from backend history and then merge websocket prices into the latest 5-minute bucket.
- Updated [README.md](/Users/guozhen_wu/Documents/vibe-code-test/README.md) to document the new history endpoint and session-aware chart behavior.

#### Result
Price panels now load with seeded intraday context, session starts are handled per asset category instead of one global day boundary, and live prices continue updating the chart after the initial history load.

### Reason
These changes were made to move the chart panels from a browser-only approximation to a backend-defined session-aware history model that is much closer to a real market dashboard.

## 2026-03-30 15:44:02

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> ECharts Price Panel Hover Upgrade

### STAR

#### Situation
The custom SVG chart tooltip behavior was still fragile near panel boundaries, and repeated manual positioning fixes were making the price panel interaction more complex without producing reliable hover behavior.

#### Task
Replace the fragile custom hover implementation with a charting layer that can handle line rendering, crosshair hover, and boundary-aware tooltip placement more robustly inside the price panel.

#### Action
- Added `echarts` to the frontend dependencies in [package.json](/Users/guozhen_wu/Documents/vibe-code-test/frontend/package.json).
- Reworked the expanded price chart in [MarketCard.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/components/MarketCard.vue) to use `ECharts` for the `1D` line, hover tooltip, and axis pointer instead of the custom SVG hover logic.
- Kept the surrounding card layout, `H/L/C` side markers, and seeded-history data flow intact while letting `ECharts` handle interactive hover behavior.

#### Result
The price panel now uses a more robust charting engine for hover interactions, with cleaner tooltip behavior and less fragile manual positioning logic at the chart edges.

### Reason
These changes were made to stop spending effort on brittle custom tooltip positioning and move the chart interaction onto a more reliable visualization foundation.

## 2026-03-30 16:05:39

### <span style="color:#d14343;font-weight:700;">FIX</span> Frontend Interaction And Layout Regressions

- Fixed the ticker card and alarm drawer interaction so closing the alarm drawer restores the expanded panel width instead of leaving the layout in an inconsistent state.
- Fixed the Fear & Greed panel collapsed state so it persists after opening and closing the alarm drawer.
- Fixed multiple dashboard UI regressions around the settings menu surface, delete actions, state coloring, daypart indicator styling, and ticker/alarm visual cues.
- Hardened the chart lifecycle and hover behavior to reduce disappearing chart states and tooltip edge issues after the ECharts migration.

## 2026-03-30 16:12:40

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Macro Sidebar And Platform Roadmap Export

### STAR

#### Situation
The dashboard still lacked a dedicated macro market surface for key US futures, and the broader platform roadmap ideas we discussed were only in chat rather than captured in the repository.

#### Task
Add a dedicated macro section for major US futures in the sidebar and export the next-platform feature ideas into a standalone todo file.

#### Action
- Added built-in macro contracts for `VX=F`, `ES=F`, and `NQ=F` in [dependencies.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/api/dependencies.py) so the backend fetches them alongside tracked tickers without mixing them into the main watchlist.
- Extended [MarketSnapshot](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/models/market.py) to carry `macro_tickers`, and updated [App.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/App.vue) plus the new [MacroPanel.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/components/MacroPanel.vue) to render a dedicated `Market Macro` sidebar section.
- Exported the platform roadmap ideas into [TODO-LIST.md](/Users/guozhen_wu/Documents/vibe-code-test/TODO-LIST.md).

#### Result
The page now has a dedicated compact macro futures section for VIX, Nasdaq, and S&P futures, and the broader platform direction is captured in a separate todo file inside the repo.

### Reason
These changes were made to make the dashboard more useful as a market-monitoring platform while preserving the roadmap discussion in a durable project artifact.

### <span style="color:#d14343;font-weight:700;">FIX</span> Stock Session History Anchoring

- Updated [market_service.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/services/market_service.py) to anchor the seeded `1D` history window to the latest available market point instead of the current wall-clock time.
- This prevents equities like `NVDA` from falling into an empty current-day session window outside live market hours and improves history seeding when markets are closed.

## 2026-03-30 16:17:03

### <span style="color:#d14343;font-weight:700;">FIX</span> Yahoo Macro Symbol And Previous Close Handling

- Replaced the failing Yahoo macro symbol `VX=F` with `^VIX` in [dependencies.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/api/dependencies.py), and relabeled it as `VIX Index` in the macro feed.
- Updated [yfinance_provider.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/providers/yfinance_provider.py) so quote fetching falls back from `1m` to `5m` intraday data when necessary instead of returning no market data immediately.
- Updated [yfinance_provider.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/providers/yfinance_provider.py) so `previous_close` is resolved from daily trading-day history rather than the previous intraday bar, which improves change calculations for equities like `NVDA`.

## 2026-03-30 16:30:00

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Workflow Roadmap Capture Rules

### STAR

#### Situation
We were already using the skill to keep changelog and API artifacts in sync, but idea-only discussions still depended on memory and were not consistently being written into the project roadmap with a visible implementation state.

#### Task
Extend the workflow so both user and Codex ideas are captured in the todo list whenever they are discussed but not implemented, and formalize todo items with explicit implementation status markers.

#### Action
- Updated [SKILL.md](/Users/guozhen_wu/Documents/vibe-code-test/skills/change-delivery-workflow/SKILL.md) so the workflow now requires new unimplemented ideas to be added to [TODO-LIST.md](/Users/guozhen_wu/Documents/vibe-code-test/TODO-LIST.md) and existing items to be marked as `Not Yet Implemented` or `Implemented`.
- Updated [openai.yaml](/Users/guozhen_wu/Documents/vibe-code-test/skills/change-delivery-workflow/agents/openai.yaml) so the skill metadata matches the new changelog, Postman, and roadmap expectations.
- Normalized existing roadmap items in [TODO-LIST.md](/Users/guozhen_wu/Documents/vibe-code-test/TODO-LIST.md) to use explicit status labels.

#### Result
The workflow now captures idea-only discussions in a durable roadmap file, and the todo list shows implementation state clearly instead of behaving like an unstructured note dump.

#### Reason
These changes were made so future planning discussions stay visible in the repository even when we intentionally postpone implementation.

## 2026-03-30 16:48:00

### <span style="color:#d14343;font-weight:700;">FIX</span> Price Panel Five-Minute Change Semantics

- Added explicit `five_min_change` and `five_min_change_percent` fields to [MarketQuote](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/models/market.py) so the backend can carry both previous-close change and true five-minute change without mixing the two.
- Updated [yfinance_provider.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/providers/yfinance_provider.py) and [mock_provider.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/providers/mock_provider.py) so the `5m` metrics are calculated from the previous completed 5-minute close instead of the prior trading-day close.
- Updated [cache.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/core/cache.py) and [MarketCard.vue](/Users/guozhen_wu/Documents/vibe-code-test/frontend/src/components/MarketCard.vue) so the price panel now displays the real five-minute delta and percent change while leaving the broader session change fields intact for other UI surfaces.
