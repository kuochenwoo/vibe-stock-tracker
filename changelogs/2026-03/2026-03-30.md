# 2026-03-30

## 2026-03-30 08:56:14

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Modular Backend And Managed Tickers

### STAR

#### Situation
The backend market fetch path was failing, the project structure was too concentrated, and tracked instruments were still tied to hard-coded assumptions.

#### Task
Make the app behave like a real project by modularizing the backend, separating providers, and supporting managed tracked tickers.

#### Action
- Refactored the backend into clearer `api`, `core`, `models`, `providers`, `repositories`, and `services` layers.
- Added a provider factory and moved market access behind provider abstractions.
- Replaced hard-coded ticker placeholders with managed tracked tickers and added ticker management endpoints and frontend support.
- Updated Postman and README to match the backend contract.

#### Result
The backend became easier to extend, tracked instruments became configurable, and the app could evolve toward additional providers cleanly.

### Reason
These changes were made to move the app from a prototype structure to a maintainable project architecture.

## 2026-03-30 09:47:03

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Dashboard Frontend Expansion

### STAR

#### Situation
The frontend layout was cramped, management flows were overly static, and the app needed a more serious dashboard experience.

#### Task
Reshape the frontend into a cleaner dashboard while improving ticker management, alarm workflows, accessibility on a LAN, and market sentiment visibility.

#### Action
- Reworked the page header, settings entry point, ticker management modal, and alarm workflows.
- Added LAN-friendly frontend/backend access changes and improved browser-side notification handling.
- Added the Fear & Greed gauge and related backend/frontend support.
- Introduced a sidebar/news placeholder structure and refined the overall dashboard composition.

#### Result
The app became more usable as a dashboard, with better layout, better controls, and stronger support for live monitoring and future expansion.

### Reason
These changes were made to turn the UI into a more practical monitoring surface instead of a rough prototype.

## 2026-03-30 11:35:41

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Ticker Card And Interaction Polish

### STAR

#### Situation
The card design, alarm entry flow, modal presentation, and collapsed ticker watchlist behavior still needed significant refinement to feel usable and visually coherent.

#### Task
Polish the frontend interactions and visual structure, especially the ticker cards and compact watchlist behavior.

#### Action
- Reworked the ticker cards, chart treatment, price markers, and compact/collapsed states.
- Moved alarm creation into per-ticker controls and refined the drawer workflow.
- Simplified and restyled the tracked tickers modal, hints, and settings controls.
- Refined collapsed watchlist alignment so prices, controls, and ticker badges behave more like a finance watchlist.

#### Result
The frontend now behaves more like a focused market dashboard, with cleaner card interactions and a more intentional compact ticker layout.

### Reason
These changes were made to improve usability, readability, and visual consistency across the core market-monitoring flows.

## 2026-03-30 13:47:10

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Daily Changelog Workflow

### STAR

#### Situation
The changelog process was creating too many low-signal entries, and the repository was still relying on a single growing root changelog file.

#### Task
Reduce changelog noise, keep logs easier to navigate, and define a durable structure for future entries.

#### Action
- Updated the workflow skill to batch minor related tweaks into larger changelog entries when appropriate.
- Replaced the monolithic root changelog with a root index plus daily log files under month-based folders.
- Preserved the current history as daily files for March 2026.

#### Result
The project now has a cleaner changelog workflow and a file structure that is easier to maintain over time.

### Reason
These changes were made to keep project history readable and to avoid a single oversized changelog file.

## 2026-03-30 13:55:45

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Database Infrastructure Planning

### STAR

#### Situation
The project needed to move from JSON-based tracked ticker storage toward a more durable architecture, starting with Postgres for ticker definitions and Redis for short-lived market reference values.

#### Task
Create the initial infrastructure files needed to stand up Postgres and Redis locally, define the Postgres schema for tracked tickers and aliases, and document the Redis key design for runtime market state.

#### Action
- Added [docker-compose.yml](/Users/guozhen_wu/Documents/vibe-code-test/docker-compose.yml) with local Postgres and Redis services, health checks, and persistent volumes.
- Added [001_market_schema.sql](/Users/guozhen_wu/Documents/vibe-code-test/infra/postgres/init/001_market_schema.sql) to define `tracked_tickers` and `ticker_aliases`, including seed records for crude oil and gold futures.
- Added [redis-key-design.md](/Users/guozhen_wu/Documents/vibe-code-test/docs/redis-key-design.md) to document the runtime Redis keys for latest quotes and previous completed 5-minute closes.
- Updated [README.md](/Users/guozhen_wu/Documents/vibe-code-test/README.md) with the new local database infrastructure entry points.

#### Result
The repository now has a concrete local database stack definition and a first-pass data design for both durable ticker metadata and short-lived Redis market state.

### Reason
These changes were made to establish a clear starting point for moving tracked tickers into Postgres and 5-minute reference values into Redis before wiring the application code to those stores.

## 2026-03-30 14:11:43

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Postgres And Redis Backend Integration

### STAR

#### Situation
The repository already had the database design and local infrastructure files, but the running backend still used a JSON file for tracked tickers and did not actually write runtime quote state into Redis.

#### Task
Replace the JSON-backed ticker persistence with Postgres, connect the backend to Redis for latest quote and previous completed 5-minute close storage, and preserve the current API behavior for the frontend.

#### Action
- Added [database.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/core/database.py) to manage Postgres connections and bootstrap the schema.
- Added [cache.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/core/cache.py) to write latest quotes and previous completed 5-minute closes into Redis.
- Replaced the JSON ticker repository with a Postgres-backed [ticker_repository.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/repositories/ticker_repository.py).
- Extended [market.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/models/market.py) and [ticker_service.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/services/ticker_service.py) to support provider, aliases, and metadata on tracked tickers.
- Updated [market_service.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/services/market_service.py), [dependencies.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/api/dependencies.py), and [main.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/main.py) so the app initializes Postgres-backed ticker storage and writes runtime market cache state into Redis.
- Updated [yfinance_provider.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/providers/yfinance_provider.py) and [mock_provider.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/providers/mock_provider.py) so each quote now carries the previous completed 5-minute close metadata needed for Redis.
- Added `psycopg` and `redis` dependencies in [requirements.txt](/Users/guozhen_wu/Documents/vibe-code-test/backend/requirements.txt) and updated [README.md](/Users/guozhen_wu/Documents/vibe-code-test/README.md) to document the new runtime configuration.

#### Result
The backend now persists tracked tickers in Postgres and writes runtime market cache values into Redis, while keeping the existing ticker and market APIs intact for the frontend.

### Reason
These changes were made to move the backend onto the intended database architecture so tracked instruments are durable and runtime quote references are stored in an appropriate fast cache.

## 2026-03-30 14:15:03

### <span style="color:#d14343;font-weight:700;">FIX</span> Postgres Ticker Metadata Insert

### STAR

#### Situation
After moving tracked tickers into Postgres, creating a new ticker through `POST /api/tickers` could fail with `psycopg.ProgrammingError` because the repository was passing a raw Python `dict` into a JSONB column.

#### Task
Fix the ticker insert path so request metadata is written to Postgres in a format `psycopg` can adapt.

#### Action
- Updated [ticker_repository.py](/Users/guozhen_wu/Documents/vibe-code-test/backend/app/repositories/ticker_repository.py) to wrap ticker metadata with `Jsonb(...)` before inserting into the `tracked_tickers.metadata` column.

#### Result
The backend can now insert new tracked tickers with JSON metadata without throwing the JSON adaptation error.

### Reason
These changes were made to restore working ticker creation after the Postgres migration.

## 2026-03-30 14:17:29

### <span style="color:#1b8f59;font-weight:700;">FEAT</span> Formalized Changelog Entry Types

### STAR

#### Situation
The changelog workflow already supported daily files and batched updates, but it did not yet define a formal distinction between feature entries and bug-fix entries.

#### Task
Standardize the changelog format so feature work is grouped into one main feature entry and bug fixes are grouped into concise fix entries.

#### Action
- Updated [SKILL.md](/Users/guozhen_wu/Documents/vibe-code-test/skills/change-delivery-workflow/SKILL.md) to define `<span style="color:#1b8f59;font-weight:700;">FEAT</span>` entries for grouped feature work and `<span style="color:#d14343;font-weight:700;">FIX</span>` entries for grouped bug fixes.
- Updated [openai.yaml](/Users/guozhen_wu/Documents/vibe-code-test/skills/change-delivery-workflow/agents/openai.yaml) so the workflow metadata matches the new categorized changelog format.
- Updated [CHANGELOG.md](/Users/guozhen_wu/Documents/vibe-code-test/CHANGELOG.md) to document the new changelog rules in the root index.

#### Result
The repository now has a formal changelog convention: grouped feature work is recorded as a main `FEAT` entry with STAR, and grouped bug fixes are recorded as concise `FIX` entries.

### Reason
These changes were made to make the changelog easier to scan and more consistent across larger features and smaller bug-fix batches.

## 2026-03-30 14:18:49

### <span style="color:#d14343;font-weight:700;">FIX</span> Historical Changelog Labels

- Updated the existing March changelog entries to use the new categorized labels.
- Converted grouped feature batches to `<span style="color:#1b8f59;font-weight:700;">FEAT</span>` headings.
- Converted the Postgres ticker insert bug entry to a `<span style="color:#d14343;font-weight:700;">FIX</span>` heading.
